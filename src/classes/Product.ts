export class Product {
    private name: string;
    private price: number;
    // z akej restauracie

    constructor(name: string, price: number) {
        this.name = name;
        this.price = price;
    }
}